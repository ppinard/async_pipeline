[bdist_wheel]
python-tag=py3

[tool:pytest]
norecursedirs = .* build dist CVS _darcs *.egg venv old
addopts = --cov --cov-report xml

[versioneer]
VCS = git
style = pep440
versionfile_source = async_pipeline/_version.py
versionfile_build = async_pipeline/_version.py
tag_prefix =
parentdir_prefix = async_pipeline-

